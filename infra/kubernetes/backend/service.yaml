apiVersion: v1
kind: Service
metadata:
  name: socializer-backend
  labels:
    app: socializer-backend
spec:
  type: ClusterIP
  selector:
    app: socializer-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
